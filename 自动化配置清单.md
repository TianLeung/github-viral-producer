# WorkBuddy 自动化生产线 · 配置清单

> 三个 Skill 已安装完毕，本文件只解决「第 1 步数据源」和「第 4 步串联」在 Automation UI 里怎么配。
> 最后更新：2026-09-14

---

## 0. 先决条件：配置 GitHub Token（强烈建议，否则跑不动）

未鉴权限流 **60 次/小时**，且按**出口 IP** 计算，共享网络下极易被别人打满（本机构建时就已触发 403）。

1. 打开 https://github.com/settings/tokens → **Generate new token (classic)**
2. **不需要勾选任何 scope**，只填备注和有效期即可
3. 复制 Token，在终端执行：

```bash
echo 'export GITHUB_TOKEN=ghp_你的Token粘贴到这里' >> ~/.zshrc && source ~/.zshrc
```

4. 验证：`echo $GITHUB_TOKEN`

---

## 1. 新建 Automation（定时任务）

| 配置项 | 值 |
|---|---|
| 名称 | GitHub 开源爆款生产线 |
| 触发器 | 定时运行 |
| 频率 | 每周一、周三、周五 |
| 时间 | 10:00 |
| Cron（如 UI 支持） | `0 10 * * 1,3,5` |

---

## 2. 方案 A：单节点全 AI 驱动（推荐 ⭐）

只用**一个** AI 任务节点，让它自己按编排 Skill 跑完三步。
优点：不受 HTTP 节点能力限制，编排 Skill 里已内置降级、限流提示、落盘路径。

**节点类型**：AI 任务 / 调用 Skill
**引用 Skill**：`github-viral-pipeline`（v2，含本地知识库加载与三步洗稿）
**任务提示词**（整个复制）：

```
执行 GitHub 开源爆款短视频生产线 v2：
1. 读取 /Users/liangtian/Downloads/公众号爆款文章/WorkBuddySkills/GitHub开源工具内容创作/
   .workbuddy/cache/ 下的提示词缓存与爆款骨架拆解
2. 运行 ~/.workbuddy/skills/github-trend-radar/scripts/fetch_trending.py
   --days 7 --min-stars 200 --limit 5 --deep --out output/radar.md
3. 用「核心共鸣洗稿引擎」(resonance-engine) 做三步洗稿过滤，产出选题提纯单
4. 用「开源爆款编导」(opensource-video-director) 按爆款骨架生成分镜表
   （时间节点/画面截图与视觉标注/口播文案三要素，前 5 秒为痛点钩子且不出现项目名）
5. 用「口语化抛光师」(colloquial-polisher) 只润色口播文案列，表格结构不变
6. 成稿保存为 output/YYYY-MM-DD-开源项目名-短视频脚本.md，并汇报推荐标题与备选选题
```

---

## 3. 方案 B：HTTP 节点 + Skill 节点（你的原方案）

### ⚠️ 你的查询式有三个必须修正的问题

| # | 问题 | 现象 | 修正 |
|---|---|---|---|
| 1 | **操作数超限** | 6 个 topic = 5 个 `OR`，4 个 `NOT` = 4 个，合计 9 个 → `422 Validation Failed: More than five AND/OR/NOT operators` | 单个请求内 `OR + NOT ≤ 5`。要么**按 topic 拆成多次请求再合并**，要么**砍到 2 个 topic + 3 个 NOT** |
| 2 | **URL 编码** | `created:>2026-09-07` 里的 `>`、`+` 在 URL 中必须编码 | `>` → `%3E`；空格用 `+` 或 `%20` |
| 3 | **🔥 纯 topic 会把爆款全漏掉（最致命）** | 2026-09-14 实测：`topic:webui created:>2026-09-01` 命中 23 条但 Star 全在 **2~5**；而 `created:>2026-09-10 stars:>100` 里 Star **646/630/607** 的项目 `topics` **全是空数组** —— 新项目根本没来得及打标签 | 改成 **双通道**：① `created:>X stars:>200` 捞热度池（主力）② topic 查询只作兜底；再用「UI 启发式打分」排序（评分规则见 Skill 文档） |

> **结论：优先用方案 A。** 抓取脚本已经把这三条全部内置处理好了（分批请求 + 双通道 + UI 打分 + 三级降级），
> HTTP 节点方案需要你在工作流里手动复刻这套逻辑，维护成本高。

### HTTP 节点配置

- **Method**：`GET`
- **URL**：`https://api.github.com/search/repositories`
- **Headers**：

| Key | Value |
|---|---|
| Accept | `application/vnd.github+json` |
| User-Agent | `WorkBuddy-Bot` |
| X-GitHub-Api-Version | `2022-11-28` |
| Authorization | `Bearer 你的GITHUB_TOKEN` |

- **Query Parameters**（拆成 3 次请求，每次 topic 不同，取回后合并按 Star 排序）：

| 批次 | q 值 |
|---|---|
| ① | `(topic:webui OR topic:gradio) created:%3E2026-09-07 stars:%3E200 NOT sdk NOT library NOT framework NOT benchmark -topic:awesome-list -topic:awesome -topic:books -topic:course -topic:tutorial -topic:interview` |
| ② | `(topic:streamlit OR topic:gui) created:%3E2026-09-07 stars:%3E200 NOT sdk NOT library NOT framework NOT benchmark -topic:awesome-list -topic:awesome -topic:books -topic:course -topic:tutorial -topic:interview` |
| ③ | `(topic:desktop-app OR topic:app) created:%3E2026-09-07 stars:%3E200 NOT sdk NOT library NOT framework NOT benchmark -topic:awesome-list -topic:awesome -topic:books -topic:course -topic:tutorial -topic:interview` |

其余参数三批相同：

| 参数名 | 参数值 |
|---|---|
| sort | `stars` |
| order | `desc` |
| per_page | `10` |

> `-topic:xxx` 这种否定限定符**不占用** 5 个操作符名额，可以放心多加。
> `{{DATE_7_DAYS_AGO}}` 请在 UI 里用日期变量表达式，输出格式必须是 `YYYY-MM-DD`。

### 提取字段（喂给下一个节点）

```
items[i].name              项目名称
items[i].html_url          项目地址
items[i].description       一句话介绍
items[i].stargazers_count  实时 Star
items[i].topics            主题标签
items[i].homepage          演示主页（可选，但强烈建议带上）
items[i].created_at        创建时间
```

### 后续节点

| 顺序 | 节点 | 技能 | 入参 |
|---|---|---|---|
| 4 | 调用 Skill | `opensource-video-director`（开源爆款编导） | 上一步提取的项目详情文本 |
| 5 | 调用 Skill | `colloquial-polisher`（口语化抛光师） | 第 4 步的完整分镜表格 |
| 6 | 输出 | 创建文档 / 发送通知 | 落盘 `output/YYYY-MM-DD-项目名.md`，或推送飞书/微信 |

---

## 4. 本地手动跑一次（不配 Automation 也能用）

```bash
cd "/Users/liangtian/Downloads/公众号爆款文章/WorkBuddySkills/GitHub开源工具内容创作"
python3 ~/.workbuddy/skills/github-trend-radar/scripts/fetch_trending.py \
  --days 7 --min-stars 200 --limit 5 --deep --out output/radar-$(date +%Y-%m-%d).md
```

然后在对话里说：「用 output 里的雷达结果跑一遍爆款生产线」。

### ✅ 已实测通过（2026-09-14，未配 Token 的限流状态下）

```
命中 5 条，模式 hybrid（热度通道 + UI 打分）
1. Mac-Duo          811 ⭐  7 分  macOS 菜单栏应用（topic 命中 menubar-app）
2. truanayangi      597 ⭐  6 分  有演示主页 + 本地运行
3. gongwen-gbt9704  587 ⭐  2 分  公文排版 Skill
4. anything2explainer 1203 ⭐ 1 分  AI 视频生成
5. rune             481 ⭐  1 分  有演示主页
```

对照：同一时间用纯 `topic` 模式查询 → **0 条结果**。产出示例见 `output/radar-2026-09-14.md`。

---

## 5. 已安装 Skill 清单

| Skill | 中文名 | 路径 |
|---|---|---|
| `github-trend-radar` | GitHub 爆款雷达 | `~/.workbuddy/skills/github-trend-radar/` |
| `resonance-engine` | 核心共鸣洗稿引擎（逻辑平移+痛点提纯） | `~/.workbuddy/skills/resonance-engine/` |
| `opensource-video-director` | 开源爆款编导 | `~/.workbuddy/skills/opensource-video-director/` |
| `colloquial-polisher` | 口语化抛光师 | `~/.workbuddy/skills/colloquial-polisher/` |
| `github-viral-pipeline` | 生产线总控 v2（编排） | `~/.workbuddy/skills/github-viral-pipeline/` |

### 本地知识库（数据流，换赛道只换这两个文件夹）

| 源文件 | 缓存（已解析） | 状态 |
|---|---|---|
| `相关参考提示词/*.pdf`（4 个） | `.workbuddy/cache/prompts-md/*.md` | ✅ 已解析 |
| `相关参考提示词/核心共鸣过滤与口语化引擎.pages` | 同目录同名 .md | ⚠️ IWA 提取，约 60% 内容可读（Pages 未安装，二进制解析有碎片） |
| `参考视频/参考视频1.mp4`（InfiniteTalk，37.6s） | `.workbuddy/cache/video-frames/骨架拆解.md` | ✅ 已拆解 |
| `参考视频/参考视频2.mp4`（腾讯 AuK TTS，25.1s） | 同上 | ✅ 已拆解 |

---

## 6. 当前状态与待办

- [x] 三个 Skill + 一个编排 Skill 已创建
- [x] 抓取脚本已通过语法、分批逻辑、真实 API 端到端验证
- [x] 修复「纯 topic 漏筛爆款」缺陷，改为双通道 + UI 打分
- [x] 本地知识库解析（4 个 PDF + 1 个 .pages）+ 参考视频爆款骨架拆解（2 个）
- [x] 新增「核心共鸣洗稿引擎」Skill，产线升级 v2（七步四阶段）
- [ ] **配置 GITHUB_TOKEN**（当前 60 次/小时，配后 5000 次/小时，且能开 `--deep`）
- [ ] 在 Automation UI 建定时任务（每周一三五 10:00，cron `0 10 * * 1,3,5`）
- [ ] 配置输出节点（本地文档 / 飞书 / 微信）
- [ ] （可选）用 Pages/WPS 打开 .pages 核对提取出的《核心共鸣过滤与口语化引擎》全文并补全缓存
