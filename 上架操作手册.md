# 上架操作手册（GitHub + WorkBuddy 专家）

> 目标：让你在**任意一台新电脑**（Windows / Mac）上打开 WorkBuddy，说一句话，就能把整套"开源爆款短视频生产线"装好并跑起来。
> 最后更新：2026-09-17

---

## ✅ 当前状态：已上线（2026-09-17 19:10）

| 项目 | 状态 |
|---|---|
| GitHub 仓库 | **https://github.com/TianLeung/github-viral-producer** |
| 可见性 | 公开（Public）✅ |
| 已推送提交 | 6 条，最新 `b5dbfb5` |
| 免 Token 下载 | ✅ 实测 1.2MB 成功 |
| 新电脑自动安装 | ✅ 实测 5/5 技能就位，当场抓到 3 个项目 |
| GITHUB_TOKEN | 已写入 `~/.zshrc`（配额 5000 次/小时） |

**所以下面 Part A 的 GitHub 部分你已经完成了**，保留是为了以后再建仓库时查。
你现在要做的只剩**两件事**：Part B 上传专家包 zip，以及文末的安全收尾。

---

## 第 0 部分 · 先搞懂这三样东西的关系

| # | 东西 | 放哪 | 作用 | 你要做的 |
|---|------|------|------|----------|
| 1️⃣ | **GitHub 仓库** `github-viral-producer` | github.com/TianLeung | 云端仓库，永久保存源码 + 两个 zip | 建一次，push 一次 |
| 2️⃣ | **专家包 zip** | `dist/` 里两个文件 | 真正的"安装包"，含 5 个技能 | 上传到 WorkBuddy 专家中心 |
| 3️⃣ | **新电脑** | 随便哪台 | 用"装配工"从 GitHub 下载 → 安装 → 执行 | 零配置，说句话就行 |

**一句话概括**：GitHub 是**仓库**，WorkBuddy 专家是**货架**，新电脑是**买家**。
买家只要装了"装配工"，就会自动去仓库把货运下来装好、然后开工。

---

# Part A · GitHub 怎么操作

## A1 · 生成 Token（相当于一把"钥匙"，只做一次）

### 步骤

1. 打开这个网址（建议直接复制粘贴到浏览器地址栏）：
   ```
   https://github.com/settings/tokens/new?description=workbuddy&scopes=repo
   ```
   *如果它跳到了新版的 Fine-grained token 页面，看下面的「备选方案」。*

2. 页面上每一项**这样填**：

   | 页面上的框 | 填什么 | 说明 |
   |---|---|---|
   | **Note**（备注名） | `workbuddy` | 随便起，自己认得就行 |
   | **Expiration**（有效期） | 选 `90 days` 或 `1 year` | ⚠️ 千万别选 "No expiration"，丢了无法吊销 |
   | **Select scopes**（权限） | **只勾 `repo` 这一项（顶层那个大勾）** | ✅ 勾上后下面 `repo:status` / `repo_deployment` 等会自动全选，这是正常的 |
   | 其他所有 scope | **一个都不要勾** | 尤其别勾 `delete_repo`（能删库的危险权限） |

3. 滚到页面最底部，点绿色的 **「Generate token」** 按钮。

4. **立刻复制**那串以 `ghp_` 开头的字符串（长这样：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`）。
   ⚠️ **这串只在生成后显示一次**，刷新页面就再也看不到了，只能删掉重生成。

5. **把它用纯文本发给我**（直接粘贴到聊天框），或者存到你自己的备忘录里。

### 备选方案：如果上面网址打开是新版页面（Fine-grained token）

| 框 | 填什么 |
|---|---|
| Token name | `workbuddy` |
| Expiration | `90 days` |
| Repository access | 选 **All repositories** |
| Permissions → Repository permissions | **Contents = Read and write**；**Metadata** 会自动 Read-only，不用管 |
| 然后 | 点 Generate token，复制 `github_pat_` 开头的串 |

> ✅ 两种 token 我们这边都能用，效果一样。

---

## A2 · 创建仓库（我可以自动做，但你要知道填了什么）

### 推荐做法：你什么都不用点，把 Token 给我，我跑一行命令全自动建好

```bash
cd "/Users/liangtian/Downloads/公众号爆款文章/WorkBuddySkills/GitHub开源工具内容创作"
python3 setup_github.py
```
脚本会自动建仓库 → push 44 个文件 → 配好环境变量 → 告诉你仓库地址。

### 手动做法（如果你想自己在网页上建）

点 GitHub 右上角 `+` → **New repository**，每项这样填：

| 页面上的框 | 填什么 | ⚠️ 为什么 |
|---|---|---|
| **Owner** | `TianLeung`（默认就是你的账号，别动） | |
| **Repository name** | `github-viral-producer` | **必须一模一样**，因为装配工里写死了这个仓库名 |
| **Description**（描述，选填） | `开源爆款短视频生产线：GitHub 嗅探 → 洗稿 → 分镜 → 去AI味 → 落盘` | |
| **Public / Private** | 选 **Public**（公开） | 🔴 **关键**：只有公开仓库，新电脑才能**免 Token 直接下载**。选私有就得每台电脑都配 Token，麻烦 |
| **Add a README file** | ⚪ **不要勾** | 本地已有 README，勾了会冲突 |
| **Add .gitignore** | ⚪ **不要勾** | 本地已有，且是定制过的 |
| **Choose a license** | ⚪ **不要勾**（或选 MIT） | 勾了同样会冲突 |

填完点 **Create repository**。
⚠️ 仓库名如果不是 `github-viral-producer`，请告诉我，我要改 `config.json`。

---

## A3 · 上传什么？（44 个文件，已经帮你筛好了）

### ✅ 要上传的（已经全部 `git add` 好了，共 44 个）

| 目录 | 内容 | 数量 | 为什么必须传 |
|---|---|---|---|
| `skills/` | 5 个技能的 SKILL.md + 抓项目脚本 | 7 | 生产线的**方法论本体** |
| `expert/` | 两个专家包的源码（plugin.json + agents + skills + 头像） | 18 | 别人能自己打包 |
| `dist/*.zip` | `github-viral-producer.zip`(520KB) + `viral-producer-bootstrap.zip`(20KB) | 2 | 🔴 **核心**：新电脑下载的就这俩 |
| `bootstrap/` | 装配工的独立副本 | 3 | 单独也能用 |
| `install.py` / `install.sh` | 跨平台安装器 | 2 | 手动装时用 |
| `setup_github.py` / `build_expert.py` / `pack_knowledge.py` | 三个自动化脚本 | 3 | 自己维护用 |
| `output/` | 4 份已跑通的成稿 + 1 份雷达报告 | 5 | **样例**，让人一看就懂产出长啥样 |
| `README.md` / `自动化配置清单.md` | 说明文档 | 2 | |
| `.gitignore` | 忽略规则 | 1 | 🔴 **保证红线文件不被误传** |

### 🔴 绝对不能上传的（.gitignore 已经拦住了，但你要知道为什么）

| 文件 / 目录 | 为什么 | 换电脑怎么办 |
|---|---|---|
| `相关参考提示词/` | 你的付费 / 私有方法论原文（PDF、Pages） | 用 `pack_knowledge.py` 打包带走 |
| `参考视频/` | 别人的爆款视频，**有版权** | 同上，单独拷 |
| `dist/knowledge-kit-*.zip` | 上两样的打包产物（4.2MB） | 网盘 / 移动硬盘 |
| `.workbuddy/memory/` | 含本机路径的工作日志 | 不用带 |
| `.DS_Store` / `__pycache__` | 系统垃圾 | 不用带 |

> ✅ **安全结论**：红线全被拦住了，所以这个仓库**可以放心设为 Public**。
> 仓库里只有"提炼后的方法论"，没有你的私有原文、没有别人的视频。

**私有知识怎么带走？** 在旧电脑跑：
```bash
python3 pack_knowledge.py
```
会生成 `dist/knowledge-kit-2026-09-17.zip`（4.2MB），拷到新电脑解压即可。

---

## A4 · 三种上传方式（选一种）

| 方式 | 怎么做 | 适合 |
|---|---|---|
| **① 给我 Token（推荐）** | 把 `ghp_xxx` 纯文本发我 → 我跑 `setup_github.py` | 最省事，30 秒搞定 |
| **② 自己敲命令** | 见下方代码块 | 想自己掌控 |
| **③ 网页拖拽** | GitHub 仓库页 → `Add file` → `Upload files` → 拖整个文件夹 | ❌ **不推荐**：一次只能传 100 个文件，且会丢目录结构 |

方式 ② 的命令（把 `ghp_你的Token` 换成真 Token）：

```bash
cd "/Users/liangtian/Downloads/公众号爆款文章/WorkBuddySkills/GitHub开源工具内容创作"
git remote add origin https://TianLeung:ghp_你的Token@github.com/TianLeung/github-viral-producer.git
git branch -M main
git push -u origin main
```

---

# Part B · WorkBuddy 专家上架什么、填什么

## B1 · 上哪个 zip？

| 文件 | 大小 | 内容 | 建议 |
|---|---|---|---|
| **`dist/github-viral-producer.zip`** | 520 KB | 完整版「爆导」，自带全部 5 个技能 | ✅ **主力，必须上** |
| `dist/viral-producer-bootstrap.zip` | 20 KB | 轻量「装配工」，只负责从 GitHub 拉+装+跑 | ✅ 建议也上（新电脑用） |

> 两个都已经**在本地注册成功了**，你现在就能在 WorkBuddy 专家列表里看到它们。

> ⚠️ **上传不是可选项，是跨设备的关键。**
> 装在本机 `~/.workbuddy/plugins/marketplaces/my-experts/plugins/` 里的专家
> **只存在这台机器的硬盘上**，登录同一个账号**不会**让它们跑到另一台电脑去。
> 想换设备还能用，只有两条路：**① 上传到专家中心（跟随账号）**，
> **② 从 GitHub 重新拉（见 `新手激活应用首段话.md`）**。建议两条都留。

## B2 · 上架时每个字段填什么（照抄这张表）

下面是「**爆导 / github-viral-producer**」的填写内容（这些值已经在 `plugin.json` 里写好了，如果 UI 是上传 zip 自动识别，就什么都不用填）：

| UI 字段 | 填这个值 | 备注 |
|---|---|---|
| **名称 / Name** | `github-viral-producer` | 英文、小写、连字符 |
| **显示名（中文）** | `爆导` | 列表里显示的名字 |
| **显示名（英文）** | `Boom` | |
| **职业 / Profession** | 中文 `开源爆款短视频制片人`<br>英文 `Open-Source Viral Short-Video Producer` | |
| **一句话简介（中文）** | `把新爆开源项目洗稿成能开拍的短视频分镜：抓项目、共鸣提纯、出分镜表、去AI味、校验语速、落盘成稿。` | |
| **一句话简介（英文）** | `Turns newly trending GitHub repos into shoot-ready short-video storyboards: radar sourcing, resonance rewriting, three-element storyboard, de-AI polish, speech-rate check, auto archiving.` | |
| **分类 / Category** | `06-ContentCreative`（内容创作） | |
| **作者名** | `TianLeung65367` | |
| **邮箱** | `tianleung65367@foxmail.com` | ⚠️ 建议和另一个包统一成 `tianleung65367@gmail.com` |
| **版本号** | `1.0.0` | |
| **头像** | `avatars/expert.png`（已在 zip 里） | 自动读取，一般不用手动传 |
| **默认开场白（中文）** | `帮我抓最近 7 天爆火的开源项目，挑最适合大众的一个，写一份能直接开拍的短视频分镜脚本。` | |
| **标签 Tags** | `开源项目选题` / `短视频分镜` / `爆款洗稿` | 3 个 |
| **快捷提示 Quick Prompts** | ① `帮我抓最近 7 天爆火的开源项目…`<br>② `照这个 GitHub 项目链接，给我出一版完整的短视频分镜脚本。`<br>③ `把这份脚本的口播去一遍 AI 味，并做 4 字/秒语速校验。` | 复制表格里的原文 |

下面是「**装配工 / viral-producer-bootstrap**」：

| UI 字段 | 填这个值 |
|---|---|
| **名称** | `viral-producer-bootstrap` |
| **显示名（中/英）** | `装配工` / `Fetch` |
| **职业** | 中文 `生产线装配工`<br>英文 `Pipeline Installer & Runner` |
| **一句话简介（中文）** | `从 GitHub 拉回爆款生产线并装好，然后直接跑：抓项目、洗稿、出分镜、落盘成稿。` |
| **分类** | `06-ContentCreative` |
| **作者名 / 邮箱** | `TianLeung` / `tianleung65367@gmail.com` |
| **标签** | `一键安装` / `跨平台` / `远程同步` |
| **默认开场白** | `把爆款生产线装到这台电脑，然后跑一遍。` |
| **快捷提示** | ① `把爆款生产线装到这台电脑，然后跑一遍。`<br>② `装好生产线，然后抓最近 7 天的开源项目跑一遍。`<br>③ `检查这台电脑的生产线装得全不全。` |

## B3 · 上传入口（如果 UI 里找不到，用兜底方案）

**正常路径**：WorkBuddy 左侧栏 → **专家** → 我的专家 / 创建专家 → **上传压缩包** → 选 `dist/` 里的 zip。

**兜底方案（100% 有效，手动注册）**：把 zip 解压后整个文件夹丢进这个目录，重启 WorkBuddy 即可：

| 系统 | 路径 |
|---|---|
| **Mac** | `~/.workbuddy/plugins/marketplaces/my-experts/plugins/` |
| **Windows** | `C:\Users\<你的用户名>\.workbuddy\plugins\marketplaces\my-experts\plugins\` |

> 你本机已经用这个方式装好了，可以在访达里按 `Shift+Cmd+G` 输入 `~/.workbuddy/plugins/marketplaces/my-experts/plugins/` 查看，
> 现在里面有 3 个文件夹：`github-viral-producer`、`viral-producer-bootstrap`、`wechat-viral-editor`。

---

# Part C · 新电脑怎么用（这是你的最终目的）

1. 新电脑装好 WorkBuddy。
2. 上传 `viral-producer-bootstrap.zip`（装配工）到专家中心 —— **只需这一个 20KB 的小包**。
3. 对它说：**「把爆款生产线装到这台电脑，然后跑一遍。」**
4. 它会自动：
   - 从 `https://github.com/TianLeung/github-viral-producer` 下载 zip
   - 自动解压、摊平 GitHub 的嵌套目录
   - 把 5 个技能装到本机 skills 目录（Mac / Windows 路径自动识别）
   - 把脚本里的占位符 `{{WORKSPACE}}` `{{SKILLS_DIR}}` `{{PYTHON}}` 替换成新电脑的真实路径
   - **直接开跑**：抓项目 → 洗稿 → 出分镜 → 去AI味 → 语速校验 → 落盘

**不需要**：装 git、配 Token、手动拷文件。只要能上网就行。

> 📋 **嫌记不住？直接看 `新手激活应用首段话.md`** —— 里面有一段现成的提示词，
> 复制到新电脑的 WorkBuddy 里回车即可，连专家包都不用提前装。

---

# Part D · 常见坑

| 现象 | 原因 | 解决 |
|---|---|---|
| 装配工说"下载失败" | ① 仓库还没 push；② 仓库是 Private；③ 仓库名不是 `github-viral-producer` | 先确认 GitHub 上能打开 `github.com/TianLeung/github-viral-producer` 且是公开的 |
| Token 生成后不见了 | GitHub 只显示一次 | 删掉重新生成一个 |
| push 报 403 | Token 没勾 `repo` | 重新生成，务必勾 `repo` |
| push 报 "repository not found" | 仓库还没建 | 先建仓库，或让我跑脚本自动建 |
| 新电脑装完技能不生效 | WorkBuddy 没重启 | 完全退出再打开 |
| 私有知识丢了 | 本来就走单独通道 | 跑 `pack_knowledge.py` 打包带走 |

---

# Part E · 以后怎么用（日常 SOP）

## E1 · 三种日常场景，一句话搞定

| 场景 | 你做什么 | 说这句话 |
|---|---|---|
| **写内容**（99% 的情况） | 什么都不用做 | 对「爆导」说：**"帮我抓最近 7 天爆火的开源项目，写一份能开拍的分镜脚本"** |
| **换/加新电脑** | 装 WorkBuddy → 导入 `viral-producer-bootstrap.zip` | 对「装配工」说：**"把爆款生产线装到这台电脑，然后跑一遍"** |
| **改了技能想同步** | 改完文件 | 对 AI 说：**"把改动同步到 GitHub"**（见 E2） |

## E2 · 改了东西怎么同步到 GitHub

以后你调整了某个 `SKILL.md`、加了新脚本、跑出了新成稿，要更新云端：

```bash
cd "/Users/liangtian/Downloads/公众号爆款文章/WorkBuddySkills/GitHub开源工具内容创作"
python3 setup_github.py
```

它会自己：建/复用仓库 → 提交 → 推送 → 报告地址。
**前提**：`~/.zshrc` 里的 `GITHUB_TOKEN` 还有效（新开终端自动生效）。

> 更简单的方式：直接跟 AI 说"同步到 GitHub"，AI 会跑上面这些。

⚠️ 同步前**不用**担心私有资产 —— `.gitignore` 拦着，`setup_github.py` 还有一道
推送前扫描（发现 pdf/pages/mp4 直接中止）。

## E3 · 内容更新后，新电脑怎么拿到最新版

新电脑不用重装，对「装配工」再说一次：
**"把爆款生产线装到这台电脑"** —— 它会拉最新代码覆盖安装（幂等，不会重复装）。

## E4 · 想回滚到之前的版本

```bash
git log --oneline          # 找那行的编号，比如 b5dbfb5
git checkout b5dbfb5 -- skills/    # 只把 skills 目录恢复到那次的状态
```

## E5 · 记住这三条就够了

1. **写内容** → 找「爆导」，其他都不用管。
2. **换电脑** → 导 20KB 的「装配工」，说一句话自动装。
3. **改东西** → 说"同步到 GitHub"。

---

## 你现在只剩两件事

### 1️⃣ 上传专家包 zip 到 WorkBuddy（见 Part B）
上传 `dist/github-viral-producer.zip` 和 `dist/viral-producer-bootstrap.zip`。
> 本机**已经装好了**，现在就能用；上传是为了换电脑后不必手动拷。

### 2️⃣ 安全收尾（建议做，5 分钟）

你刚才那串 Token 在聊天记录里是**明文**的，等于钥匙被拍了照。
建议去这里吊销它：

```
https://github.com/settings/tokens
```

点那条 `workbuddy` 右边的 **Delete** → **Revoke**。然后：

| 你想怎样 | 怎么做 |
|---|---|
| **A. 以后还要从这台 Mac 抓数据**（推荐） | 重新生成一个 Token，**一个 scope 都不勾**（只要有效就能把配额提到 5000/小时，不需要任何权限），生成后发我，我改 `~/.zshrc` |
| **B. 无所谓，60 次/小时够用** | 直接吊销，什么都不用做。产线照跑，只是抓项目时偶尔会撞限流 |

> 吊销**不会影响**已经推上去的仓库，GitHub 上的代码不受影响。
> 换台电脑从头跑也**不需要 Token**，靠的是公开仓库免密下载。
