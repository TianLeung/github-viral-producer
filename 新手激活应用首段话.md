# 新手激活应用 · 首段话

> **什么时候用这个**：任何一台"还没装过这套东西"的机器上第一次使用。
> 包括换电脑、重装系统、Mac ↔ Windows 互换，或者帮别人装。
>
> **怎么用**：装好 WorkBuddy → 把下面「第一段话」整段复制 → 粘贴进对话框 → 回车。
> 剩下的它自己干。你不用装 git，不用配任何账号密码。

---

## 第一段话：激活（每台新机器只做这一次）

```
请帮我从 GitHub 下载并安装一套技能包，然后跑一遍。

仓库地址：https://github.com/TianLeung/github-viral-producer

具体做这几件事：
1. 从这个地址下载仓库 zip 并解压（这是公开仓库，不需要账号和密码）
2. 解压后运行里面的 install.py，把 5 个技能装到我电脑的 WorkBuddy 技能目录
   （Windows 用 python install.py，Mac 用 python3 install.py）
3. 装完告诉我：技能装了几个、装在哪个文件夹、有没有报错
4. 然后跑一遍完整流程：抓最近 7 天爆火的开源项目 → 挑最合适的一个 →
   写一份完整的短视频分镜脚本 → 存到我电脑的 output 文件夹
```

它会自动下载、装好、然后直接开工。

---

## 第二段话：以后每次写内容，只说这句

```
帮我抓最近 7 天爆火的开源项目，挑最适合大众的一个，写一份能直接开拍的短视频分镜脚本。
```

---

## 如果第一段话没跑通

按下面顺序排查，把报错**原样复制**给 AI 看：

| 现象 | 大概率原因 | 怎么办 |
|---|---|---|
| 说"下载失败 / 连不上" | 网络抖动或代理 | 让它**再试一次**，多试两次通常就过了 |
| 说"找不到 python" | 这台机器没装 Python | 去 python.org 装一个，装的时候勾 **Add to PATH** |
| 说"权限不足" | Windows 用户目录限制 | 换个目录，比如装到 `D:\workbuddy-pipeline` |
| 装完 WorkBuddy 里看不到技能 | 没重启 | **完全退出 WorkBuddy 再打开** |

---

## 想让新机器"连这段话都不用说"？再传一个专家包

上面那段话已经够用了。如果你希望更省事，可以在 WorkBuddy 专家中心上传
`dist/viral-producer-bootstrap.zip`（20KB），发布成「不公开」，以后对「装配工」
说一句"把生产线装到这台电脑"就行。

> ⚠️ 注意：装在本机 `~/.workbuddy/plugins/marketplaces/my-experts/plugins/`
> 里的专家**只存在于这台机器的硬盘上**，不会因为登录了同一个账号就跑到别的电脑去。
> 想跨设备用，只有两条路：**专家中心上传**，或者**从 GitHub 重新拉**。

---

## 你的私有知识库（提示词 PDF + 参考视频）怎么办

它们**不在** GitHub 上（有版权和隐私考虑，故意没传）。

换新机器时单独带过去：

1. 在旧机器跑一次 `python3 pack_knowledge.py`，得到 `dist/knowledge-kit-日期.zip`（约 4.2MB）
2. 用 U 盘 / 网盘 / 微信把这个 zip 传到新机器
3. 解压，把里面的 `相关参考提示词` 和 `参考视频` 两个文件夹，
   放到新机器这个仓库目录的**根目录下**

不放也能跑，只是洗稿质量会退化成通用水平。建议放。

---

## 忘了怎么操作怎么办

直接打开 GitHub 仓库看这两份文档，永远是最新的：

- **新手激活应用首段话.md** ← 你正在看的这份
- **上架操作手册.md** ← GitHub 建仓 / Token / 专家上传的完整填写对照表
