# 可重建产物
.workbuddy/cache/prompts-md/
.workbuddy/cache/video-frames/   # 参考视频抽帧图：他人视频的衍生内容，有版权
**/__pycache__/
*.pyc

# 系统文件
.DS_Store
Thumbs.db

# ============================================================
# ⚠️ 私有资产 —— 永不进 git（换机走网盘 / 移动硬盘单独拷贝）
#
#   相关参考提示词/  你的付费 / 私有方法论原文（PDF、Pages）
#   参考视频/        他人的爆款视频（有版权）
#
# 这两类一旦 push 到公开仓库就收不回来了（Git 历史会永久保留）。
# 仓库里只留方法论"提炼版"（在 skills/ 里），原文走 pack_knowledge.py 打包带走。
# 因此本仓库**可以安全地设为 Public**。
# ============================================================
相关参考提示词/
参考视频/

# 知识库打包产物（含私有内容，别提交）
dist/knowledge-kit*.zip

# 个人工作日志（含本机路径与项目细节，不提交）
.workbuddy/memory/
